#include <iostream>
#include <cstdio>
#include <stdlib.h>
    #include <direct.h>
    #include <string.h>
    #include <string>


using namespace std;

int main()
{
 //ac std::string s = std::to_string(42);



    /*int i,j;

int val;
   char str[20], temp;
int a = 10;
char *intStr = itoa(a);
string std::str = string(intStr);

char name[] ="orko";
int size = sizeof(name) / sizeof(name[0])-1;
cout<<size;


for(i=0; i<size; i++){

    for(j=0; j<size; j++){

        temp = name[j];
        name[j] =name[i];
        name[i]=temp;
        mkdir(name);
        printf("%s \n", name);
    }
}

    for(int i=0; i<10; i++);
   // mkdir(number[0]);
*/
    return 0;
}