JMP lable1

label0:
mov ax, -1
hlt

lable2:
mov ax, 1
hlt



lable1:
mov ax, 7FFFH
mov bx, 8000H
 cmp bx,ax
 ja label0
 mov ax, 0













cx = 2(bx x 3 ax) - 6bx

mov ax, 5h
mov bx, 10d

add cx, ax
add cx, ax
add cx, ax 

add cx, bx  

add cx,cx
sub cx, bx
sub cx, bx
sub cx, bx
sub cx, bx
sub cx, bx
sub cx,bx